<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="/css/StyleFusion.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="https://kit.fontawesome.com/398aab1134.js" crossorigin="anonymous"></script>
</head>
<body style="background-image: url(https://365psd.ru/images/backgrounds/bg-lightl-877.jpg)">
    @*<div class="navbar-uppercase" id="NavBar">
        <div class="navbar-width-half justify-content-sm-start align-content-center">
            <a class="text-decoration-none" href="@Url.Action("Index","Home") "><h2 class="first unselectable">На главную</h2></a>
        </div>
            @if (User.Identity.IsAuthenticated)
            {<div class="navbar-width-half justify-content-end align-content-center">
                <h2 class="unselectable m-sm-3">@User.Identity.Name</h2>
                <a href="@Url.Action("PersonalArea", "Account", new { name = User.Identity.Name })" class="m-sm-3">В личный кабинет(заменить на фотку)</a>
            </div>
            }
            else
            { <div class="navbar-width-half justify-content-end align-content-center">
                    <a class="a-color m-sm-3" asp-controller="Account" asp-action="Login"><h2>Вход</h2></a>
               </div>
            }
        <div>
            <script src="~/js/scripts.js"></script>
        </div>
    </div>*@
    <header class="header">
        <div class="left">
            <a class="fiGpxA font-family-text color-orangered" href="@Url.Action("Index","Home")">
                <i class="fa-solid fa-bowl-food"></i>
            </a>
            <div class="position-relative">
                <span style="color:black; line-height: 1.2;">
                    <span style="color:black; line-height: 1; display: inline-block; white-space: pre-wrap;">
                        Доставка пиццы
                    </span>
                </span>
            </div>
        </div>
        <div class="right">   </div>
    </header>
    <div class="content">
        @RenderBody()
    </div>
</body>
</html>